# PROBLEM: 'when targetAnchor' was printed as 'when.targetAnchor' in  JavaScript
# TEST EMPTY sugar -ljs $FILE | grep 'when.targetAnchor'
@class bozo
	@method bozozo targetAnchor
		when targetAnchor == "E" 
			when  True -> return false
		when targetAnchor == "W" or targetAnchor == "NW" or targetAnchor == "N"
			return False
		end
		
		return true
	@end
@end
